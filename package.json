{
    "name": "locket-monorepo",
    "version": "0.0.0",
    "packageManager": "npm@10.9.4",
    "private": true,
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "scripts": {
        "build": "turbo run build",
        "dev": "turbo run dev",
        "lint": "turbo run lint",
        "test": "turbo run test"
    },
    "devDependencies": {
        "turbo": "^2.3.3",
        "typescript": "^5.9.3"
    }
}
